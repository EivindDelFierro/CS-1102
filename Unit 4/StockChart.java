import java.util.ArrayList;
import java.util.Scanner;

public class StockChart {
  
  public StockChart(ArrayList<Double> arrayListPrice) {
    final ArrayList<Double> INPUT_PRICES = new ArrayList<Double>();
    final Scanner SCANNER = SingletonScanner.getScanner();
    // prompt user to enter inputs
    System.out.println("Enter prices. Entering a non-numerical value will terminate the input.");

    // get input from user
    // is input a number?
      // if it is,
        // add it to our chart
        // if it isn't

  }

}
